<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.Mapper.SecretMapper">

    <select id="getSecret" resultType="java.lang.Integer">
        select exists(select 1
                      from interviewworks.secret
                      where year (created_date) = year (CURRENT_TIMESTAMP())
		                and month(created_date) = month(CURRENT_TIMESTAMP())
        ) as has_this_month;
    </select>

    <insert id="createSecret">
        insert
        into interviewworks.secret (secret_number)
        values (#{secretNum})
    </insert>

    <select id="getSecretOnly" resultType="java.lang.String">
        select secret_number as secret_number
        from interviewworks.secret
        where year (created_date) = year (CURRENT_TIMESTAMP())
          and month(created_date) = month(CURRENT_TIMESTAMP())
    </select>

</mapper>